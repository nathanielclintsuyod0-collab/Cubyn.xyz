(()=>{const c=SERAPHYX,$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)],out=$("#products");document.getElementById("year").textContent=new Date().getFullYear();$$("[data-link]").forEach(a=>a.href=c.discord);
function render(cat){let p=c.products.filter(x=>cat==="all"||x.cat===cat);out.innerHTML=p.map(x=>`<article class="product"><div class="product-art"><span>${x.icon}</span><small>${x.cat.toUpperCase()}</small></div><div class="product-body"><h2>${x.name}</h2><p>${x.desc}</p><div class="buy"><strong>${x.price}</strong><a class="btn gold" href="${x.url}" ${x.url.startsWith("http")?'target="_blank" rel="noopener"':""}>PURCHASE</a></div></div></article>`).join("")}render("all");$$(".shop-tabs button").forEach(b=>b.onclick=()=>{$$(".shop-tabs button").forEach(x=>x.classList.remove("active"));b.classList.add("active");render(b.dataset.cat)});document.querySelector(".hamburger")?.addEventListener("click",()=>document.querySelector("nav").classList.toggle("open"))})();