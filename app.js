(()=>{const c=SERAPHYX,$=s=>document.querySelector(s),$$=s=>[...document.querySelectorAll(s)];document.getElementById("year").textContent=new Date().getFullYear();$$("[data-link]").forEach(a=>a.href=c.discord);
const toast=m=>{let t=$("#toast");t.textContent=m;t.classList.add("show");setTimeout(()=>t.classList.remove("show"),2200)};async function copy(){try{await navigator.clipboard.writeText(c.serverIP);toast("Copied "+c.serverIP)}catch{toast(c.serverIP)}}$("#copy-ip")?.addEventListener("click",copy);$("#copy-ip-2")?.addEventListener("click",copy);
const eras=["commoner","adventurer","nobility","royalty","arcane","legendary"], names=["Commoner","Adventurer","Nobility","Royalty","Arcane","Legendary"],strip=$("#rank-strip"),preview=$("#rank-preview");
if(strip){strip.innerHTML=eras.map((e,i)=>`<button data-era="${e}" class="${i===0?"active":""}">${names[i]}</button>`).join("");function render(e){let a=c.ranks[e],mid=a[Math.floor(a.length/2)];preview.innerHTML=`<div><span>${names[eras.indexOf(e)]}</span><h3>${mid[0]}</h3><p>${mid[1]}</p></div>`}render("commoner");$$(".rank-strip button").forEach(b=>b.onclick=()=>{$$(".rank-strip button").forEach(x=>x.classList.remove("active"));b.classList.add("active");render(b.dataset.era)})}
const io=new IntersectionObserver(es=>es.forEach(e=>{if(e.isIntersecting){e.target.classList.add("show");io.unobserve(e.target)}}),{threshold:.12});$$(".feature,.cards article,.heading,.banner").forEach(x=>io.observe(x));document.querySelector(".hamburger")?.addEventListener("click",()=>document.querySelector("nav").classList.toggle("open"));})();